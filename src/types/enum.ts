export enum Types {
  LIKE = "LIKE",
  DISLIKE = "DISLIKE",
  SAD = "SAD",
  ANGRY = "ANGRY",
}

export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum STATUS {
  APPROVED = "APPROVED",
  PENDING = "PENDING",
  REJECTED = "REJECTED",
}

export enum DOCSTYPES {
  POST = "POST",
  COMMENT = "COMMENT",
}
